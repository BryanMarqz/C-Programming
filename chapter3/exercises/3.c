/*
 * a), b) and d) are equivalent.
 * c) is not equivalent because the trailing space will require the user to input a non-whitespace 
 * character at the end of the input signifying the end of the whitespace sequence matched by 
 * the space.
 */
